{
  "name": "angular-labs",
  "version": "1.0.0",
  "private": true,
  "description": "Oracles exécutables des labs Angular (règle : un lab = un geste métier complet). Solution GREEN / starter RED. Portée : services (DI + RxJS), testés en TypeScript pur — le rendu de template Angular (TestBed + compilateur) a été essayé (@analogjs/vite-plugin-angular + @angular/build) et écarté : segfault natif reproductible avec ce toolchain sur cet environnement, chantier séparé documenté dans le README du cours.",
  "type": "module",
  "scripts": {
    "lab:01": "vitest run --root lab-01-feature-de-zero",
    "solution:01": "vitest run --root lab-01-feature-de-zero --config vitest.solution.config.ts"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "~7.8.0",
    "typescript": "~5.6.0",
    "vite": "^5.4.10",
    "vitest": "^2.1.8"
  }
}
