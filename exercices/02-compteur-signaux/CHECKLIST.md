# Checklist — Exercice 02

- [ ] Le composant `CounterComponent` est standalone
- [ ] Un signal `count` de type `number` est declare et initialise a 0
- [ ] Un computed `double` retourne `count() * 2`
- [ ] Un computed `isEven` retourne un booleen selon la parite
- [ ] Un effect affiche la valeur du compteur dans la console à chaque changement
- [ ] Le template affiche la valeur, le double, et pair/impair avec `@if`/`@else`
- [ ] Bouton **+** incremente le compteur
- [ ] Bouton **-** decremente le compteur
- [ ] Bouton **Reset** remet le compteur a 0
- [ ] Le bouton **-** est désactivé quand `count() === 0`
- [ ] Zero `any` dans le code
- [ ] TypeScript strict : tous les signaux/computeds sont types
- [ ] Bonus : pas d'incrementation configurable (si tente)
- [ ] Bonus : couleur conditionnelle du compteur (si tente)
